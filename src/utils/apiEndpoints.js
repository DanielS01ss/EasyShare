const developmentPort = 5000;
const baseUrl = `http://localhost:${developmentPort}`;
export const LOGIN_AUTH_ENDPOINT = `${baseUrl}/auth/login`;
export const GOOGLE_LOGIN_AUTH_ENDPOINT = `${baseUrl}/auth/google-auth`;
